generated: '2026-07-20'
method: searched
source: live probe of https://api.thingiverse.com + https://www.thingiverse.com/developers
api: Thingiverse REST API (MakerBot)
base_url: https://api.thingiverse.com
conventions:
  authentication:
    style: Bearer token (OAuth 2.0 / App Token)
    header: Authorization
    scheme: Bearer
    ref: authentication/makerbot-authentication.yml
  idempotency:
    supported: false
    note: >-
      No idempotency-key header or contract is documented; the API is a standard
      REST surface over Thingiverse resources.
  cors:
    enabled: true
    allow_origin: '*'
    allow_methods: [GET, PUT, POST, DELETE, PATCH, OPTIONS]
    allow_headers: [DNT, Keep-Alive, User-Agent, X-Requested-With, If-Modified-Since, Cache-Control, Content-Type, Range, Authorization]
    expose_headers: [Content-Disposition]
    evidence: 'Access-Control-* headers observed live on api.thingiverse.com'
  versioning:
    scheme: token-versioned
    current: V1
    evidence: 'WWW-Authenticate: Bearer V1'
  error_envelope:
    format: custom-json
    shape: '{"error": string, "code": int, "type": string}'
    ref: errors/makerbot-problem-types.yml
  pagination:
    style: page-number
    params: [page, per_page]
    note: >-
      Thingiverse list endpoints accept page and per_page query parameters (per
      developer docs); response bodies were not inspected (token required).
  media_type: application/json
